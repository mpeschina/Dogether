# Test guidance

Tests must not assert fixed user-facing wording or fixed presentation timing.
Test observable behaviour, state transitions, data, and available controls instead, so
copy and pacing can change without breaking the suite.
